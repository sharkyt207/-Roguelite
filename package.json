{
  "name": "grid-forge",
  "version": "0.4.0",
  "description": "Grid-Forge — a mobile-first roguelite where your build is a spatial puzzle.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "sim": "tsx tools/balance-sim.ts"
  },
  "devDependencies": {
    "tsx": "^4.23.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.0.3"
  }
}
